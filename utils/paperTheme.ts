import { DefaultTheme } from "react-native-paper";

export const paperTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#ebaa39",
    accent: "#e74043",
    // Customize other colors as needed
  },
};
