name: CI

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22.19.0
          cache: npm

      - run: npm ci

      # google-services.json and assets/data/ are gitignored; stub them so
      # imports resolve for tsc and jest (app code only reads the shape).
      - name: Stub gitignored local files
        run: |
          echo '{"client":[{"oauth_client":[{"client_id":""}]}]}' > google-services.json
          mkdir -p assets/data
          for f in $(grep -oh 'assets/data/[A-Za-z0-9_-]*\.json' utils/loadPremadePlans.ts | sort -u); do
            [ -f "$f" ] || echo '[]' > "$f"
          done

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint:ci

      - name: Test
        run: npx jest --ci
